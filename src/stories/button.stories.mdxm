import { Meta, Story, Canvas } from '@storybook/addons-docs';

import { Button } from '../';

<Meta title="MarkDownButton" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button Story

Some stories for buttonStype

<Story
    name="Marked Story"
    args={{

    }}>
    {Template.bind({})}

</Story>
